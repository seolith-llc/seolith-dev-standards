# SEOlith Engineering Standard v2.0 -- documented exception (STANDARD.md exceptions table).
# RULE<TAB>path[:line] glob<TAB>expiry<TAB>reason
M2	.github/workflows/legacy/*	2026-09-30	legacy workflow directory -- inert (GitHub reads only files directly in .github/workflows/); delete-by date required in .github/workflows/legacy/README.md
